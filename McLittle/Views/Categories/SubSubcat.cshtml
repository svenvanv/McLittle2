@model IEnumerable<McLittle.Models.SubSubCategory>

@{
    ViewBag.Title = "SubSubcat";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.ActionLink("Products", "Products", new { id = item.SubSubCategoryId }) |
                @Html.ActionLink("Details", "Details", new { id = item.SubSubCategoryId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.SubSubCategoryId })
            </td>
        </tr>
    }

</table>
